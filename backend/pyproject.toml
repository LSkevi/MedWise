[project]
name = "medwise-api"
version = "1.0.0"
description = "MedWise medication recommendation API"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "httpx>=0.27.0",
    "pydantic>=2.0.0",
]
